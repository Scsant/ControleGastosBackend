# Arquivo de exemplo das variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Configurações do Django
SECRET_KEY=django-insecure-p9&$2kie5$y0=gp9)6id4pnw-**%cbbfkx31nq($s%37=y74un
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Configuração do banco de dados (deixe vazio para usar SQLite em desenvolvimento)
# DATABASE_URL=postgres://usuario:senha@localhost:5432/nome_do_banco

# Para deploy no Heroku, as variáveis serão configuradas automaticamente
# Não é necessário configurar DATABASE_URL para Heroku
